/* eslint-disable no-undef */

/**
 * Group F
 * Should result in 2/2
 */
describe('F', () => {
  it('Foo', () => {
    expect(true).to.equal(true);
  });

  it('Bar', () => {
    expect(true).to.equal(true);
  });
});
