/* eslint-disable no-undef */

describe('extra', () => {
  describe('E', () => {
    it('Foo', () => {
      expect(true).to.equal(false);
    });

    it('Bar', () => {
      expect(true).to.equal(true);
    });
  });
});
