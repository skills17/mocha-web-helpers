/* eslint-disable no-undef */

describe('Extra', () => {
  describe('D', () => {
    it('Foo', () => {
      expect(true).to.equal(true);
    });

    it('Bar', () => {
      expect(true).to.equal(true);
    });

    it('Baz', () => {
      expect(true).to.equal(true);
    });
  });
});
